import Component from "../../Component";

export default class Obstacle extends Component {
    public properties: {
        type: string;
    }

    constructor(props) {
        super(props);
    }
}