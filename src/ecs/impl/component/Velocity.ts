import Component from "../../Component";

export default class Velocity extends Component {
    public properties: {
        speed: number;
    };
    constructor(props) {
        super(props);
    }
}